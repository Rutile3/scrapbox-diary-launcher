# Scrapbox Diary Launcher 改修TODO

この文書は、`docs/REFACTOR_PLAN.md` に基づく今後の作業を、レビューしやすく後戻り可能なコミット単位で管理するためのチェックリストです。

原則として、各項目を1コミットとして実施します。作業中に独立した不具合や追加課題が見つかった場合は、関係のない変更を混ぜず、別コミットまたは新しいTODOとして扱います。

## 進行状況

- [x] 1. GitHub Wikiをリポジトリ内へ忠実に移行する
- [x] 2. 共通ロジックと自動テストを追加する
- [ ] 3. 正規ランチャーを実装する
- [ ] 4. 旧エンドポイントを互換ラッパー化する
- [ ] 5. 生成処理とテストを追加する
- [ ] 6. ジェネレーターUIを追加する
- [ ] 7. 改名に伴う移行ガイドを追加する
- [ ] 8. READMEと移行済みWikiを現行仕様へ更新する
- [ ] 9. 最終検証結果を反映する（必要な場合のみ）

---

## 1. GitHub Wikiをリポジトリ内へ忠実に移行する

想定コミット:

```text
【文書移行】GitHub Wikiをdocs/wikiへ移行
```

作業内容:

- Wikiリポジトリを取得する
- 全ページを `docs/wiki/` へ移行する
- Wiki内リンクをリポジトリ相対のMarkdownリンクへ変換する
- `docs/wiki/README.md` に索引を作成する
- 元のページタイトル／ファイル名と移行先ファイルの対応表を作成する
- この段階では内容を現行仕様へ書き換えず、原文と用語を保持する

完了条件:

- 取得できたすべてのWikiページがリポジトリ内に存在する
- ページの欠落がなく、移行元と移行先を追跡できる
- Wikiを取得できなかった場合は、内容を創作せず、阻害要因を記録する

## 2. 共通ロジックと自動テストを追加する

想定コミット:

```text
【機能追加】日付処理とScrapbox URL生成を共通化
```

作業内容:

- ISO形式と旧短縮形式の日付を解析する
- 入力日付を厳密に検証する
- `YYMMDD`／`YYMM` のページ名を生成する
- Scrapboxプロジェクト名を検証する
- Scrapboxの遷移先URLを構築する
- 月間予定本文を生成する
- パッケージマネージャーを必要としない軽量な自動テストを追加する

完了条件:

- 通常日、うるう日、無効日、月境界、年境界をテストしている
- 月間予定本文の既存の意味が維持されている
- 日本語と特殊文字を含む本文のエンコードをテストしている

## 3. 正規ランチャーを実装する

想定コミット:

```text
【機能追加】統合Diaryランチャーを実装
```

作業内容:

- ルートの `index.html` を正規エントリーポイントにする
- `action` によって4機能を振り分ける
- `project` を必須にする
- `date=YYYY-MM-DD` と `month=YYYY-MM` を処理する
- `#body=...` をクライアント側で解析する
- 最終的なScrapbox URLへリダイレクトする
- 不足または不正な入力に対してエラー／ヘルプを表示する

完了条件:

- `show-today`、`create-today`、`show-month`、`create-month-schedule` が正規URLから動作する
- `project` がない場合にテスト用プロジェクトへ転送されない
- 自由記述の本文がGitHub Pagesへのクエリ文字列に含まれない

## 4. 旧エンドポイントを互換ラッパー化する

想定コミット:

```text
【互換対応】従来のDiaryエンドポイントをランチャーへ転送
```

作業内容:

- 既存の4つのHTMLパスを維持する
- `project_url`、`yymmdd`、`yymm`、`body` を新形式へ変換する
- 本文を可能な限りURLフラグメントへ移す
- 重複している旧JavaScriptを削除または最小化する
- 互換パラメーター変換をテストする

完了条件:

- 改名後のリポジトリ内で4つの旧パスが機能する
- 旧パスに日付処理やScrapbox URL構築のロジックが重複していない
- `/RedirectPage/` を含む外部URLの問題とは区別されている

## 5. 生成処理とテストを追加する

想定コミット:

```text
【機能追加】ランチャー連携コードの生成機能を追加
```

作業内容:

- 正規ランチャーURLを生成する
- Scrapbox UserScript／JavaScriptスニペットを生成する
- PowerShellコマンド／スクリプトを生成する
- BAT互換ランチャーを生成する
- 各形式のエスケープ処理をテストする

完了条件:

- すべての生成形式が正規ランチャーを呼び出す
- 日本語、空白、改行、`&`、`%`、`#`、`?`、`=`、引用符をテストしている
- ユーザー固有の本文や予定がリポジトリへ保存されない

## 6. ジェネレーターUIを追加する

想定コミット:

```text
【画面追加】ランチャージェネレーターUIを追加
```

作業内容:

- project、action、任意の日付／月、本文の入力欄を追加する
- 各生成結果を表示する
- 生成結果をコピーできるようにする
- 最小限のCSSで分かりやすく整える

完了条件:

- フレームワークや外部CDNに依存していない
- 入力した本文をログやWeb Storageへ保存しない
- 主要なブラウザー操作を手動確認できる手順がある

## 7. 改名に伴う移行ガイドを追加する

想定コミット:

```text
【文書追加】RedirectPageからの移行手順を追加
```

作業内容:

- `docs/MIGRATION.md` を作成する
- 新旧GitHub Pages URLの対応例を記載する
- 4つの旧エンドポイントごとに移行例を記載する
- Scrapbox UserScript、PowerShell、BAT、ブックマークの更新箇所を案内する
- `/RedirectPage/` が自動転送されるとは限らないことを明記する

完了条件:

- リポジトリ外にある呼び出し元の手動更新方法が分かる
- 代替の `RedirectPage` リポジトリ作成を前提にしていない

## 8. READMEと移行済みWikiを現行仕様へ更新する

想定コミット:

```text
【文書更新】READMEと利用手順を新ランチャーへ対応
```

作業内容:

- READMEを現在のプロジェクト名と目的に合わせて書き直す
- 対応アクションとURLパラメーターを説明する
- ジェネレーターの使用方法を説明する
- `body` のセキュリティ／プライバシー上の注意を記載する
- 移行ガイドとリポジトリ内Wikiへリンクする
- 忠実な移行後のWikiについて、古い説明を追跡可能な形で更新する

完了条件:

- 基本的な利用方法がGitHub Wikiに依存していない
- Wikiの忠実な移行と現行化が別コミットとして区別されている

## 9. 最終検証結果を反映する（必要な場合のみ）

想定コミット:

```text
【品質改善】ブラウザー検証結果を反映
```

作業内容:

- 実ブラウザーで4機能を確認する
- GitHub Pages配下で相対パスを確認する
- コピー操作を確認する
- 生成されたPowerShell／BATをWindowsで確認する
- 自動化できない確認事項と結果を文書化する
- 検証で見つかった軽微な不具合を修正する

完了条件:

- `docs/REFACTOR_PLAN.md` の品質チェックをすべて実施または文書化している
- 修正が不要でテスト実行だけの場合は、不要なコミットを作成しない
- 独立した不具合は原因ごとに別コミットまたは新しいTODOへ分ける

---

## 運用方法

- 作業依頼では、原則としてこのTODOの項目番号を指定する
- 各作業の開始時に、依存する前項目の状態と作業ツリーを確認する
- 作業完了後、該当チェックボックスを更新する
- コミットはユーザーから明示的に依頼された場合のみ行う
- pushやリモート設定の変更は、ユーザーから明示的に依頼された場合のみ行う
