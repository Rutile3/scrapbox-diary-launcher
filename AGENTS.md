# AGENTS.md

## プロジェクト

リポジトリ: `Rutile3/scrapbox-diary-launcher`

このリポジトリの旧名称は `RedirectPage` です。

このプロジェクトは、複数の呼び出し元（Scrapbox UserScript、ブラウザーのリンク、Windowsスタートアップスクリプト、BAT／PowerShell、ブックマークなど）と、日付ベースのScrapbox日記ページをつなぐ静的なGitHub Pagesツールです。

単なるリダイレクトページ集ではありません。日付計算、Scrapbox URL構築、ページ本文生成、ランチャー生成を一元化・簡素化し、呼び出し元をできる限り薄く保つことが目的です。

変更前に次を確認してください。

1. `README.md`
2. `docs/task-list.md`
3. 変更内容に対応する `docs/` 配下の文書
4. 対象となる既存ソースとテスト

`docs/REFACTOR_PLAN.md` は統合ランチャー初回改修の完了済み計画を記録した履歴資料です。現行タスクの指示としては使用しません。

---

## 開発原則

### 1. 構造を改善する前に動作を維持する

現在の実装には主に次の4機能があります。

- 今日の日記ページを表示する
- 任意の本文を指定して、今日の日記ページを作成または開く
- 現在または指定した月のページを表示する
- 本文を生成して、月間予定ページを作成または開く

既存のページ命名規則を維持してください。

- 日次ページ: `YYMMDD`
- 月次ページ: `YYMM`

リファクタリング計画で明示的に変更されていない限り、現在の月間予定本文の意味を維持してください。既存動作を暗黙に削除しないでください。

### 2. 静的かつ軽量に保つ

プレーンなHTML、CSS、JavaScriptを優先してください。

合理的に実現できない具体的要件がない限り、フレームワーク、バンドラー、パッケージマネージャー、ビルド手順、サーバー、実行時依存関係を導入しないでください。GitHub Pagesに適した状態を維持する必要があります。

### 3. ロジックを一元化する

次の処理を複数ページに重複させないでください。

- URLパラメーターの解析
- 日付の解析と検証
- ScrapboxプロジェクトURLの構築
- 日次／月次ページ名の生成
- 本文のエンコード
- 月間予定の生成
- リダイレクトの実行

必要に応じて共有関数やモジュールを作成してください。ただし、過剰に設計しないでください。

### 4. 呼び出し元を薄く保つ

Scrapbox UserScript、BAT、PowerShell、ブックマーク、スタートアップスクリプトは、基本的にランチャーURLを構築して開くだけにしてください。可能な限り、業務ロジックと日付ロジックはこのリポジトリ内に置いてください。

### 5. URL互換性を明示的な移行課題として扱う

このリポジトリは `RedirectPage` から `scrapbox-diary-launcher` に改名されています。

リポジトリ改名後に、旧GitHub PagesプロジェクトサイトのURLも自動的にリダイレクトされるとは考えないでください。リポジトリやWikiのリダイレクトと、GitHub PagesプロジェクトサイトのURLは別です。

可能な限り、新しいリポジトリ内でも次の既存パスを維持してください。

- `Scrapbox/Diary/CreateToday/CreateToday.html`
- `Scrapbox/Diary/ShowToday/ShowToday.html`
- `Scrapbox/Diary/CreateMonthSchedule/CreateMonthSchedule.html`
- `Scrapbox/Diary/ShowMonthSchedule/ShowMonthSchedule.html`

旧ファイルは、新しい統合ランチャーへ転送する薄い互換ラッパーにして構いません。

ただし、新リポジトリ内の互換ラッパーだけでは、URLに `/RedirectPage/` を含む旧Pages URLの動作は保証できません。外部呼び出し元の移行手順を文書化してください。

新しい `RedirectPage` リポジトリを自動作成しないでください。

### 6. 機密性のある本文を不必要に公開しない

`action`、`project`、`date`／`month` など、機密性のない設定はクエリ文字列で渡して構いません。

自由記述の `body` は、GitHub Pagesへのリクエストに含まれるクエリではなく、URLフラグメント（`#...`）で渡すことを優先してください。例:

`?action=create-today&project=Example#body=<エンコード済み本文>`

ランチャーはブラウザー内で最終的なScrapbox URLを構築できます。

ユーザー入力は必ず正しくエンコードしてください。本文をログへ出力したり、明示的要件なしにlocalStorageやsessionStorageへ保存したりしないでください。

### 7. 標準のURL解析機能を使用する

`URL`、`URLSearchParams` などのブラウザーAPIを優先してください。互換性上の要件がない限り、`split("&")` や `split("=")` の繰り返しでクエリ文字列を手動解析しないでください。

### 8. エラーを明確にする

`project` がないとき、テスト用Scrapboxプロジェクトへ暗黙にフォールバックしないでください。

必須パラメーターが欠けている、または無効な場合は、小さく分かりやすいエラーページを表示するなど、問題を明確に認識・診断できる方法で失敗させてください。意図しないプロジェクトにコンテンツを作成しないでください。

### 9. リモートで破壊的な操作を行わない

ローカルリポジトリのファイルを対象として作業してください。ユーザーから明示的な依頼がない限り、次を行わないでください。

- リポジトリの削除またはアーカイブ
- 代替リポジトリの作成
- リポジトリ設定の変更
- GitHub Pagesの無効化
- 既存GitHub Wikiの削除
- GitHubへのpush
- force-push
- 公開済み履歴の書き換え

明示的な依頼がない限りコミットせず、レビュー可能な作業ツリーのdiffとして残してください。

### 10. 既存のローカル作業を尊重する

編集前に次を行ってください。

- `git status` を実行する
- 未コミットの変更を確認する
- 関係のないローカル変更を上書きしない

ユーザーの既存変更がリファクタリングと競合する場合は、その変更を保持し、競合を報告してください。

---

## ドキュメントに関する規則

### 作業管理

- 作業開始時に `docs/task-list.md` を確認し、対象タスクと現在の状態を把握する
- 新しい作業は開始前に `docs/task-list.md` へ追加する
- 作業に合わせて「進行中」「完了」「保留」の状態を更新する
- タスクリストには作業状態を記録し、詳細な変更履歴を混在させない

### 変更記録

コードを変更した場合は、変更内容を次のいずれかへ日本語で記録してください。

- 機能や仕様の追加: `docs/spec_additions.md`
- 既存仕様や外部動作の変更: `docs/spec_changes.md`
- 不具合の修正: `docs/bug_fixes.md`
- 外部動作を変えない内部構造の改善: `docs/refactoring.md`
- 使用方法、セットアップ、動作環境の変更: `README.md` の該当箇所

上記に適切な記録先がない場合は、用途に合った文書を `docs/` 配下へ作成し、この節と `docs/README.md` に用途を追記してください。

`README.md` は利用者向け文書とし、内部実装の細かな変更履歴は記載しないでください。

現在、このプロジェクトにPythonコードはありません。Pythonコードを導入する必要が生じた場合は、実装前に `docs/coding-standards.md` を作成し、その規約に従ってください。

### 編集品質

- 改行崩れ、タブ混入、インデントずれ、意図しないエスケープ文字の混入を確認する
- 確認済みの箇所を、関係のない作業のついでに変更しない
- 仮説だけで周辺を広く変更せず、依頼範囲に必要な最小限の変更に留める

### Wikiの移行履歴

旧GitHub Wikiはプロジェクト履歴の一部であり、通常のリポジトリファイルへ移行する必要があります。

Wikiを独立したGitリポジトリとして取得してください。例:

`https://github.com/Rutile3/scrapbox-diary-launcher.wiki.git`

ネットワークにアクセスできない場合は、ユーザーが用意したclone済みWikiディレクトリを探してください。

Wikiへアクセスできない場合:

- 内容を創作しない
- 可能な範囲でソースコード分析を続ける
- Wiki移行が未完了であることを明確に報告する

Wikiを取得できた場合:

1. すべてのWikiページを一覧化する
2. `docs/wiki/` 配下にMarkdownファイルを作成する
3. 最初の移行では元の情報と用語を維持する
4. Wiki内リンクをリポジトリ相対のMarkdownリンクへ変換する
5. 索引用の `docs/wiki/README.md` を作成する
6. 元のページタイトル／ファイル名と移行先ファイルの対応表を記録する
7. 忠実な移行後に限り、明確に古い内容を新アーキテクチャへ合わせる
8. 実質的な訂正は、履歴を暗黙に書き換えず、追跡可能かつ明示的に行う

通常のドキュメントがGitHub Wikiへ依存しないよう、メインの `README.md` を更新してください。

この作業では元のWikiを削除または無効化しないでください。移行確認後、必要に応じてユーザーが実施します。

---

## 品質チェック

最低限、次を確認してください。

- 可能な範囲でJavaScriptの構文チェックを行う
- 代表的な日次／月次の日付をテストする
- 有効・無効な日付をテストする
- `project` がない場合と無効な場合をテストする
- 日本語、および空白、`&`、`%`、`#`、`?`、`=`、改行を本文に含めてテストする
- 生成されたランチャーURLをテストする
- 生成されたScrapbox UserScriptをテストする
- 生成されたPowerShellをテストする
- 生成されたBAT出力をテストする
- 月間予定本文を確認する
- 改名後のリポジトリに互換ラッパーのパスが存在することを確認する
- 新しい経路で、機密性のある本文がGitHub Pagesへのクエリ文字列に含まれないことを確認する

自動化できないブラウザー確認や手動確認は文書化してください。

---

## スコープの規律

要件を満たす最小限のアーキテクチャを優先してください。

汎用的な自動化フレームワークへ発展させないでください。主な対象は、ユーザーのScrapbox日記ワークフローです。
